# Review Report â€” fixture `receipt-not-utf8`

Inherited from check-receipt.py's calibration suite; the prose is never parsed, only the single fenced json block below.

## Receipt

```json
ÿþ{"tier": 1, "tier_reason": "x", "verdict": "review-clear", "lenses": {"correctness": {"status": "pass", "findings": []}}}
```
